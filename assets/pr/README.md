# PR walkthrough screenshots

Screenshots used by the "See it on a pull request" section of the top-level README.
Captured from a live demo PR (deph-remediate-test #2), scanned with `emphereio/deph-action@v0`.

Expected files (drop the PNGs here, keep these exact names):

| file | comment to screenshot |
|---|---|
| `verdict.png`      | the auto "deph — what's in your execution path" comment |
| `fix-path.png`     | the auto "deph fix path" comment |
| `investigate.png`  | the `@deph <question>` reply ("Winner: cryptography …") |
| `triage.png`       | the `@deph triage` reply |
| `threat-model.png` | the `@deph threat-model` reply |
| `injection.png`    | the injection-probe reply ("Injected instruction detected") |
